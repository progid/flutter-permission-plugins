enum LocationPermissionLevel {
  location,
  locationWhenInUse,
}

enum PermissionStatus {
  unknown,
  denied,
  granted,
  restricted,
}

enum ServiceStatus {
  unknown,
  disabled,
  enabled,
  notApplicable,
}
